# راهنمای استقرار دائمی تجارت‌یار (Render / Docker)

این راهنما برای **در دسترس بودن دائمی** سامانه است (برخلاف لینک موقت Colab که با بسته‌شدن تب قطع می‌شود).
سه راه پیشنهاد می‌شود: **Render** (ساده‌ترین و بدون مدیریت سرور)، **Docker روی سرور خودتان**، و **Docker روی VPS/Hetzner**.

> ⚠️ هر دو روش روی یک اصل مهم سوارند: کارهای سنگین در **رشته‌های پس‌زمینه** اجرا می‌شوند، بنابراین سرور باید **تک‌پردازه (single worker)** با چند thread بماند — در فایل‌های آماده این‌طور تنظیم شده و **نباید** به چند worker تغییرش دهید.

---

## ۱) استقرار روی Render (پیشنهادی)

Render تصویر Docker را می‌سازد، دیسک دائمی را سوار می‌کند و Health Check را روی `/health` نگه می‌دارد.

### پیش‌نیاز
- حساب **Render** و اتصال مخزن GitHub شما.
- **پلن پولی (Starter به بالا)** — چون پلن رایگان Render **دیسک دائمی** ندارد.

### مراحل
1. فایل `render.yaml` از قبل در مخزن آماده است (Blueprint دائمی با دیسک).
   - می‌خواهید **رایگان** تست کنید؟ به‌جای آن از `render-free.yaml` استفاده کنید
     (پلن `free`، بدون دیسک دائمی — خروجی‌ها با هر Deploy پاک می‌شوند).
   - در Render: **New → Blueprint** و مخزن را انتخاب کنید؛ اگر هر دو فایل
     (`render.yaml` و `render-free.yaml`) را دارید، فقط Blueprint مورد نظر را تأیید کنید.
2. در Render: **New → Blueprint** و مخزن `Setayesh-Jafari/tejaratyar` را انتخاب کنید.
3. Render `render.yaml` را می‌خواند و سرویس **tejaratyar** را با دیسک `/data` می‌سازد.
4. در تب **Settings** مطمئن شوید:
   - **Health Check Path** = `/health`
   - **Auto-Deploy** از `main` روشن باشد.
   - **Region** = Frankfurt (نزدیک‌ترین به ایران).
5. بعد از اولین Build، لینک `https://tejaratyar.onrender.com` می‌گیرید — همان را برای دیگران بفرستید.

> نکته: Render متغیر `PORT` را خودش تزریق می‌کند؛ نیازی به تنظیم دستی آن نیست.
> `DATA_DIR=/data` یعنی پرونده‌ها روی دیسک دائمی ذخیره می‌شوند و با هر Deploy از بین نمی‌روند.

---

## ۲) استقرار با Docker روی سرور خودتان

برای یک VPS/Linux که Docker دارد:

### با docker compose (پیشنهادی)
```bash
git clone --branch main https://github.com/Setayesh-Jafari/tejaratyar.git
cd tejaratyar
docker compose up -d --build
```
- برنامه روی پورت `8000` بالا می‌آید → `http://YOUR_SERVER_IP:8000`
- داده‌ها در volume دائمی `tejaratyar_data` نگهداری می‌شوند.
- با `restart: unless-stopped`، بعد از ریاستارت سرور هم خودش بالا می‌آید.

### ساخت و اجرای مستقیم
```bash
docker build -t tejaratyar .
docker run -d --name tejaratyar -p 8000:7860 \
  -e DATA_DIR=/data \
  -v tejaratyar_data:/data \
  --restart unless-stopped \
  tejaratyar
```

### Health Check
```bash
curl http://127.0.0.1:8000/health
# → {"ok":true,"storage":"ok", ...}
```

---

## ۳) فایل‌های استقرار چیست؟

| فایل | کاربرد |
|------|--------|
| `Dockerfile` | تصویر امن: کاربر غیر-root، `HEALTHCHECK`، `PORT`، `DATA_DIR` |
| `docker-entrypoint.sh` | اطمینان از قابل‌نوشتن‌بودن دیسک سوارشده، سپس اجرای gunicorn |
| `render.yaml` | Blueprint کامل Render (پلن پولی) با دیسک، env و Health Check |
| `render-free.yaml` | Blueprint Render روی پلن **رایگان** — بدون دیسک، خروجی موقتی |
| `docker-compose.yml` | استقرار یک‌دستوری روی سرور خودتان با volume دائمی |
| `.dockerignore` | جلوگیری از ورود `.venv`/`.git`/`jobs`/`outputs` به تصویر |

---

## ۴) متغیرهای محیطی

| متغیر | پیش‌فرض | توضیح |
|-------|---------|-------|
| `PORT` | `5000` (محلی) / تزریق Render | پورت HTTP |
| `DATA_DIR` | مسیر پروژه | ریشه‌ی ذخیرهٔ `jobs` و `outputs` — در استقرار دائمی روی دیسک |
| `MAX_ACTIVE_JOBS` | `3` | حداکثر پروندهٔ هم‌زمان |
| `JOB_TTL_HOURS` | `24` | مدت نگهداری پرونده‌ها پیش از پاک‌سازی |
| `APP_TIMEZONE` | `Asia/Tehran` | منطقهٔ زمانی گزارش‌ها |

---

## ۵) پرسش‌های رایج

**چرا تک‌پردازه؟**
پرونده‌ها در یک `JOBS` دیکشنری داخل حافظهٔ همان پردازه ثبت می‌شوند و worker در همان پردازه اجرا می‌شود. اگر چند worker داشته باشیم، یک کاربر ممکن است به worker ای برسد که پرونده‌اش را نمی‌شناسد. threadها همزمانی را تأمین می‌کنند.

**اگر پلن رایگان Render استفاده کنم؟**
برنامه کار می‌کند، ولی بدون دیسک دائمی هر Deploy دادهٔ پرونده‌ها را پاک می‌کند. برای تست موقتی کافی است؛ برای استفادهٔ واقعی پلن پولی یا Docker روی سرور خودتان بزنید.

**چطور لینک را برای نفر دیگر بفرستم؟**
لینک `https://tejaratyar.onrender.com` (یا `http://SERVER:8000`) را بفرستید. برخلاف Colab، این لینک **همیشه روشن** است و به تب یا Runtime وابسته نیست.

---

## ۶) پیش از رفتن به Production — یادآوری امنیتی
- پشت یک **HTTPS** قرار بگیرید (Render خودکار؛ برای Docker از Caddy/Nginx/Traefik یا پروکسی معکوس).
- در صورت نیاز، پشت آن احراز هویت یا VPN بگذارید تا هر کسی نتواند سرور شما را برای جستجوی سنگین استفاده کند (`MAX_ACTIVE_JOBS` را هم کم کنید).
- توکن هر پرونده خصوصی است و در هدر `X-Job-Token` ارسال می‌شود؛ لینک پرونده را بی‌توکن به اشتراک نگذارید.
